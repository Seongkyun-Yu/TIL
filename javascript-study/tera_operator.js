const array = [];
let text = array.length === 0 
    ? '배열이 비어있습니다.' 
    : '배열이 비어있지 않습니다';

console.log(text)


const condition1 = false;
const codnition2 = false;

const value = condition1
    ? '와우!'
    : codnition2
        ? 'blabal'
        : 'foo'

console.log(value)